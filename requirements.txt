fastapi>=0.115.0
uvicorn[standard]>=0.34.0
pydantic>=2.8.0
httpx>=0.27.0
litellm>=1.40.0
loguru>=0.7.0
python-dotenv>=1.0.0
asyncio-throttle>=1.0.2
aiofiles>=23.0.0
python-multipart>=0.0.6
gunicorn>=21.0.0
starlette>=0.41.0

# Performance and caching
cachetools>=5.3.0  # 用于内存缓存

# Monitoring and metrics
prometheus-client>=0.20.0  # 用于metrics收集
psutil>=5.9.0  # 用于系统监控

# Optional: For advanced features
redis>=5.0.0  # 可选：用于分布式缓存和限流
structlog>=23.0.0  # 可选：用于结构化日志

# Development and testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
httpx>=0.27.0  # 用于测试
