# =============================================
# Service Configuration
# =============================================
# 运行环境 (development / staging / production)
SERVICE_ENVIRONMENT=production
# 监听的主机地址, 0.0.0.0 表示监听所有网络接口
SERVICE_HOST=0.0.0.0
# 服务端口
SERVICE_PORT=8000
# Gunicorn工作进程数 (用于生产环境)
SERVICE_WORKERS=1
# 日志级别 (DEBUG, INFO, WARNING, ERROR, CRITICAL)
SERVICE_LOG_LEVEL=INFO
# 跨域请求来源, 逗号分隔
SERVICE_CORS_ORIGINS="*"

# =============================================
# Gemini API Configuration - [REQUIRED]
# =============================================
# ‼️ [必需] 请替换为您自己的 Gemini API 密钥, 多个密钥用逗号隔开.
# 重要: 即使有多个密钥，也不要在外部使用引号包裹.
# 正确示例: GEMINI_API_KEYS=key1,key2
# 错误示例: GEMINI_API_KEYS="key1,key2"
GEMINI_API_KEYS=your-google-ai-api-key-1,your-google-ai-api-key-2

# (可选) 如果需要，为Gemini API调用设置代理URL，否则留空
GEMINI_PROXY_URL=
# (可选) 其他高级Gemini设置
GEMINI_COOLING_PERIOD=300
GEMINI_REQUEST_TIMEOUT=120
GEMINI_HEALTH_CHECK_INTERVAL=60
MAX_RETRIES_PER_REQUEST=3

# =============================================
# Security Configuration - [REQUIRED]
# =============================================
# ‼️ [必需] 请生成并替换为您自己的安全密钥, 用逗号分隔.
# 推荐使用命令生成: openssl rand -hex 32
# 重要: 不要在外部使用引号包裹.
SECURITY_ADAPTER_API_KEYS=your-secure-client-key

# (推荐) 用于管理端点的管理员密钥, 用逗号分隔
# 重要: 不要在外部使用引号包裹.
SECURITY_ADMIN_API_KEYS=your-secure-admin-key

# =============================================
# (可选) 日志和工作目录
# =============================================
# Claude Code工具模拟器的工作目录
CLAUDE_CODE_WORKING_DIR="./claude_workspace"
